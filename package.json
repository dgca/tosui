{
  "name": "tosui",
  "private": true,
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "f:lib": "pnpm -F @tosui/react",
    "f:docs": "pnpm -F docs",
    "dev:docs": "concurrently -n docs,storybook -c blue,magenta \"pnpm -F docs start\" \"pnpm -F @tosui/react storybook\"",
    "build:site": "pnpm -F @tosui/react build && pnpm -F @tosui/react storybook:build && pnpm -F docs build && pnpm -F docs copy-storybook",
    "test": "echo \"Error: no test specified\" && exit 1",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm f:lib build && changeset publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.15.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "concurrently": "^9.2.1"
  }
}

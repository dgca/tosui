---
phase: 03-selection-controls
plan: 01
type: execute
subsystem: ui
---

<objective>
Create Select dropdown component following established Input patterns.

Purpose: Provide a styled dropdown select that matches other form controls.
Output: Select component exported from @tosui/react with size, variant, disabled, and invalid states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-typography-text-inputs/02-02-SUMMARY.md

**Key files:**
@packages/react/src/components/Input/Input.tsx
@packages/react/src/components/Input/input.module.css

**Established patterns:**
- Form input size config: sm (h=8), md (h=10), lg (h=12)
- Focus ring pattern: border-color change + box-shadow with subtle color
- Input uses CSS module for focus-visible, disabled, and invalid states

**Constraining decisions:**
- Input component patterns should be followed for consistency
- Polymorphic component with Box wrapper
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Select component</name>
  <files>packages/react/src/components/Select/Select.tsx, packages/react/src/components/Select/select.module.css, packages/react/src/components/Select/index.tsx</files>
  <action>
Create Select component following Input patterns:
- Create Select.tsx with same size/variant/isDisabled/isInvalid props as Input
- Use same sizeConfig (sm: h=8, md: h=10, lg: h=12)
- Use same variant styles (outline/filled)
- Default element: "select"
- Add custom chevron icon using CSS (background-image or ::after pseudo-element)
- Remove default browser select styling with appearance: none
- Create select.module.css with focus-visible, disabled, invalid states matching input.module.css
- Add custom dropdown arrow styling
- Create index.tsx barrel export
  </action>
  <verify>pnpm f:lib typecheck && pnpm f:lib build</verify>
  <done>Select component created with TypeScript types, CSS module styles, no build errors</done>
</task>

<task type="auto">
  <name>Task 2: Export Select from library</name>
  <files>packages/react/src/index.tsx</files>
  <action>
Add Select export to main library entry:
- Export Select component
- Export SelectProps, SelectSize, SelectVariant types
  </action>
  <verify>pnpm f:lib build && grep -q "Select" packages/react/dist/index.d.ts</verify>
  <done>Select exported from @tosui/react, visible in TypeScript declarations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib typecheck` passes
- [ ] `pnpm f:lib build` succeeds
- [ ] Select component matches Input API patterns
- [ ] Focus ring, disabled, invalid states work correctly
- [ ] Custom dropdown arrow displays (no browser default)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Select follows same patterns as Input (size, variant, states)
</success_criteria>

<output>
After completion, create `.planning/phases/03-selection-controls/03-01-SUMMARY.md`
</output>

---
phase: 03-selection-controls
plan: 02
type: execute
subsystem: ui
---

<objective>
Create Checkbox and Radio toggle components with optional labels.

Purpose: Provide styled toggle controls for form selections (single and multiple choice).
Output: Checkbox and Radio components exported from @tosui/react.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-selection-controls/03-01-SUMMARY.md

**Key files:**
@packages/react/src/components/Input/Input.tsx
@packages/react/src/components/Label/Label.tsx

**Established patterns:**
- Form input size config: sm, md, lg
- Focus ring pattern: border-color change + box-shadow
- isDisabled and isInvalid state props
- Required asterisk uses aria-hidden

**Constraining decisions:**
- Follow Input disabled/invalid patterns
- Optional label prop (renders inline label when provided)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Checkbox component</name>
  <files>packages/react/src/components/Checkbox/Checkbox.tsx, packages/react/src/components/Checkbox/checkbox.module.css, packages/react/src/components/Checkbox/index.tsx</files>
  <action>
Create Checkbox component:
- Props: size (sm/md/lg), isDisabled, isInvalid, isChecked (controlled), defaultChecked (uncontrolled), label (optional string)
- Use native input[type="checkbox"] with custom styling
- Hide native checkbox with opacity: 0, position: absolute
- Create custom visual box with check icon (using CSS ::before or SVG)
- Size config: sm (16px box), md (20px box), lg (24px box)
- When label provided, render as inline-flex with label text to the right
- Use label element as wrapper for click area
- Focus ring on custom box when input focused
- Checked state: primary-default background, white checkmark
- Create checkbox.module.css with focus-visible, disabled, invalid, checked states
- Create index.tsx barrel export
  </action>
  <verify>pnpm f:lib typecheck && pnpm f:lib build</verify>
  <done>Checkbox component created with custom styling, optional label, state handling</done>
</task>

<task type="auto">
  <name>Task 2: Create Radio component</name>
  <files>packages/react/src/components/Radio/Radio.tsx, packages/react/src/components/Radio/radio.module.css, packages/react/src/components/Radio/index.tsx</files>
  <action>
Create Radio component:
- Props: size (sm/md/lg), isDisabled, isChecked (controlled), defaultChecked (uncontrolled), label (optional string), name (for grouping), value
- Use native input[type="radio"] with custom styling
- Hide native radio with opacity: 0, position: absolute
- Create custom visual circle with inner dot (using CSS ::before)
- Same size config as Checkbox
- When label provided, render as inline-flex with label text to the right
- Focus ring on custom circle when input focused
- Checked state: primary-default border with inner dot
- Create radio.module.css with focus-visible, disabled, checked states
- Create index.tsx barrel export
  </action>
  <verify>pnpm f:lib typecheck && pnpm f:lib build</verify>
  <done>Radio component created with custom styling, optional label, state handling</done>
</task>

<task type="auto">
  <name>Task 3: Export Checkbox and Radio from library</name>
  <files>packages/react/src/index.tsx</files>
  <action>
Add exports to main library entry:
- Export Checkbox component and CheckboxProps type
- Export Radio component and RadioProps type
  </action>
  <verify>pnpm f:lib build && grep -q "Checkbox" packages/react/dist/index.d.ts && grep -q "Radio" packages/react/dist/index.d.ts</verify>
  <done>Checkbox and Radio exported from @tosui/react, visible in TypeScript declarations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib typecheck` passes
- [ ] `pnpm f:lib build` succeeds
- [ ] Checkbox shows custom styled box with checkmark when checked
- [ ] Radio shows custom styled circle with dot when checked
- [ ] Both support optional inline labels
- [ ] Focus states, disabled states work correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Custom styled toggles (not browser defaults)
- Optional label support works
</success_criteria>

<output>
After completion, create `.planning/phases/03-selection-controls/03-02-SUMMARY.md`
</output>

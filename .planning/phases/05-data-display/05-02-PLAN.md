---
phase: 05-data-display
plan: 02
type: execute
subsystem: ui
---

<objective>
Create Card component - content container with optional sections.

Purpose: Provide flexible content container with header, body, footer patterns.
Output: Card component with composable sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files:**
@packages/react/src/components/Box/Box.tsx

**Established patterns:**
- Box wrapper with semantic props
- CSS Modules for styling
- Compound component patterns

**Constraining decisions:**
- Simple Card with children, no forced header/body/footer structure
- Optional CardHeader, CardBody, CardFooter subcomponents for convenience
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Card component</name>
  <files>packages/react/src/components/Card/Card.tsx, packages/react/src/components/Card/card.module.css, packages/react/src/components/Card/index.tsx</files>
  <action>
Create Card as content container:

1. Card.tsx:
   - Card: Main container with bg=surface, border=thin, borderColor=border, rounded=md, overflow=hidden
   - CardHeader: Optional header section with px=4, py=3, borderBottom=thin (when used)
   - CardBody: Main content area with p=4
   - CardFooter: Optional footer section with px=4, py=3, borderTop=thin (when used)
   - shadow prop (none/sm/md, default sm)
   - All sections are optional - Card can just have children

2. card.module.css:
   - Card base styles
   - Section styling (header/body/footer borders)

3. index.tsx: Export Card, CardHeader, CardBody, CardFooter and types

Pattern: Flexible card container with optional compound sections.
  </action>
  <verify>pnpm f:lib build succeeds</verify>
  <done>Card component created with Header/Body/Footer subcomponents</done>
</task>

<task type="auto">
  <name>Task 2: Export Card from library</name>
  <files>packages/react/src/index.tsx</files>
  <action>
Add export for Card:
- export { Card, CardHeader, CardBody, CardFooter, type CardProps } from "./components/Card";
  </action>
  <verify>pnpm f:lib build succeeds, Card importable from @tosui/react</verify>
  <done>Card exported from @tosui/react</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib build` succeeds
- [ ] Card renders with surface background and border
- [ ] CardHeader/Body/Footer sections work independently
- [ ] Shadow variants work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Card is composable with optional sections
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-display/05-02-SUMMARY.md`
</output>

---
phase: 05-data-display
plan: 04
type: execute
subsystem: ui
---

<objective>
Create List/ListItem components and add Phase 5 examples to App.tsx.

Purpose: Complete Phase 5 with semantic list components and visual verification.
Output: List/ListItem components, App.tsx with all Phase 5 examples, phase complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files:**
@packages/react/src/components/Box/Box.tsx
@packages/react/src/App.tsx

**Established patterns:**
- Box wrapper with semantic props
- VStack for vertical lists
- CSS Modules for styling
- App.tsx section pattern

**Constraining decisions:**
- Playground verification required per project constraints
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create List and ListItem components</name>
  <files>packages/react/src/components/List/List.tsx, packages/react/src/components/List/list.module.css, packages/react/src/components/List/index.tsx</files>
  <action>
Create List as semantic list container:

1. List.tsx:
   - List: Container using ul/ol element, props: as (ul/ol, default ul), spacing (gap between items, default 0), styleType (none/disc/decimal, default none for ul, decimal for ol)
   - ListItem: Individual list item using li element
   - ListIcon: Optional icon prefix for list items (display inline-flex, mr=2)
   - Use VStack internally for spacing if spacing > 0

2. list.module.css:
   - List styling (list-style-type, margin/padding reset)
   - ListItem styling
   - ListIcon styling

3. index.tsx: Export List, ListItem, ListIcon and types

Pattern: Semantic list with optional styling and icons.
  </action>
  <verify>pnpm f:lib build succeeds</verify>
  <done>List/ListItem/ListIcon components created</done>
</task>

<task type="auto">
  <name>Task 2: Export List and add Phase 5 examples to App.tsx</name>
  <files>packages/react/src/index.tsx, packages/react/src/App.tsx</files>
  <action>
1. Add export for List to index.tsx:
   - export { List, ListItem, ListIcon, type ListProps, type ListItemProps } from "./components/List";

2. Add Phase 5 examples to App.tsx:
   - Add imports for Avatar, Card, CardHeader, CardBody, CardFooter, Image, List, ListItem, ListIcon
   - Add section types: "avatar", "card", "image", "list"
   - Add navigation entries
   - Add sections with examples:

**Avatar examples:**
- Size variants (sm, md, lg, xl)
- With image src
- With initials fallback (name only)
- Rounded variants

**Card examples:**
- Basic card with content
- Card with Header/Body/Footer
- Card with shadow variants
- Card with image

**Image examples:**
- Basic image
- With objectFit variants
- With fallback (intentionally broken src)
- With rounded variants

**List examples:**
- Unordered list
- Ordered list
- List with spacing
- List with icons
  </action>
  <verify>pnpm f:lib build succeeds</verify>
  <done>List exported, all Phase 5 components visible in playground</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md and ROADMAP.md for Phase 5 completion</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
Update project state:
- STATE.md: Update current phase to 6, mark Phase 5 complete, update progress, add any decisions
- ROADMAP.md: Mark Phase 5 as complete with plan count and date
  </action>
  <verify>cat .planning/STATE.md | grep -q "Phase: 6"</verify>
  <done>Project state reflects Phase 5 completion, ready for Phase 6</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib build` succeeds
- [ ] All Phase 5 components visible in dev server
- [ ] Avatar, Card, Image, List all functional
- [ ] STATE.md and ROADMAP.md updated
- [ ] Phase 5 complete
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- All 4 Phase 5 components have examples
- Project state updated to Phase 6
- Phase 5: Data Display complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-display/05-04-SUMMARY.md`
</output>

---
phase: 01-layout-primitives
plan: 03
type: execute
---

<objective>
Create Container component for max-width centered content layouts.

Purpose: Container provides a consistent way to constrain content width and center it horizontally, common for page layouts and content sections.
Output: Container.tsx exported from @tosui/react
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@packages/react/src/components/Box/Box.tsx
@packages/react/src/styles/styles.css
@packages/react/src/index.tsx

**Prior plan context:**
01-01 created Stack family, 01-02 created Flex/Grid - same wrapper pattern applies.

**Design tokens reference (from styles.css):**
Breakpoints: sm=640px, md=768px, lg=1024px, xl=1280px, 2xl=1536px
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Container component with size presets</name>
  <files>packages/react/src/components/Container/Container.tsx, packages/react/src/components/Container/index.tsx</files>
  <action>
Create Container component that wraps Box:
- Props:
  - size: "sm" | "md" | "lg" | "xl" | "2xl" | "full" (default "lg")
  - centerContent: boolean (centers children with flex, default false)
  - px: horizontal padding (default 4 = 16px)
- Size presets map to maxW values:
  - sm: 640px, md: 768px, lg: 1024px, xl: 1280px, 2xl: 1536px, full: 100%
- Always: mx="auto" for centering, w="100%" for full width within max
- When centerContent=true: display="flex", flexDirection="column", alignItems="center"
- Support polymorphic `as` prop
- Export ContainerProps type
- Spread ...rest onto Box
- DO NOT add CSS module - pure prop mapping with size config
  </action>
  <verify>
pnpm f:lib typecheck passes
File exists at packages/react/src/components/Container/Container.tsx
  </verify>
  <done>Container component created with size presets (sm/md/lg/xl/2xl/full) and centerContent option.</done>
</task>

<task type="auto">
  <name>Task 2: Export Container from package</name>
  <files>packages/react/src/index.tsx</files>
  <action>
Add Container export to main index.tsx:
- Add: export { Container, type ContainerProps } from "./components/Container"
  </action>
  <verify>
pnpm f:lib typecheck passes
pnpm f:lib build succeeds
  </verify>
  <done>Container exported from package entry point.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib typecheck` passes
- [ ] `pnpm f:lib build` succeeds without errors
- [ ] Container exported from @tosui/react
- [ ] Container centers itself with mx="auto"
- [ ] Size presets correctly constrain max-width
- [ ] centerContent option works for centering children
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Container provides intuitive max-width and centering API
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-primitives/01-03-SUMMARY.md`
</output>

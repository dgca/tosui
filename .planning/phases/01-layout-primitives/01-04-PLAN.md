---
phase: 01-layout-primitives
plan: 04
type: execute
---

<objective>
Create Divider and Spacer components for layout spacing utilities.

Purpose: Divider provides visual separation between content sections. Spacer fills available flex space for push-apart layouts.
Output: Divider.tsx, Spacer.tsx exported from @tosui/react
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@packages/react/src/components/Box/Box.tsx
@packages/react/src/styles/styles.css
@packages/react/src/index.tsx

**Prior plan context:**
01-01 through 01-03 created Stack, Flex, Grid, Container - same wrapper pattern applies.

**Design tokens reference:**
- border color: use "border" or "border-muted" from semantic colors
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Divider component</name>
  <files>packages/react/src/components/Divider/Divider.tsx, packages/react/src/components/Divider/index.tsx</files>
  <action>
Create Divider component that wraps Box:
- Props:
  - orientation: "horizontal" | "vertical" (default "horizontal")
  - color: color token (default "border-muted")
  - thickness: number 1-4 (default 1, maps to border width in px)
- Horizontal divider: w="100%", h={thickness}px via CSS variable, bg={color}
- Vertical divider: h="100%", w={thickness}px via CSS variable, bg={color}
- Default element: <hr> for horizontal, <div> for vertical (use conditional as default)
- Add role="separator" and aria-orientation for accessibility
- Support polymorphic `as` prop
- Export DividerProps type
- Spread ...rest onto Box
- Use inline style for thickness: style={{ height: `${thickness}px` }} or width for vertical
  </action>
  <verify>
pnpm f:lib typecheck passes
File exists at packages/react/src/components/Divider/Divider.tsx
  </verify>
  <done>Divider component created with horizontal/vertical orientation and customizable color/thickness.</done>
</task>

<task type="auto">
  <name>Task 2: Create Spacer component and export both</name>
  <files>packages/react/src/components/Spacer/Spacer.tsx, packages/react/src/components/Spacer/index.tsx, packages/react/src/index.tsx</files>
  <action>
Create Spacer component that wraps Box:
- Props: None specific (just Box props via rest)
- Always: flex={1} to fill available space
- Use case: Push items apart in flex containers (e.g., logo on left, nav on right)
- Default element: <div>
- Support polymorphic `as` prop
- Export SpacerProps type
- Very simple component - just Box with flex={1}

Then export both from main index.tsx:
- Add: export { Divider, type DividerProps } from "./components/Divider"
- Add: export { Spacer, type SpacerProps } from "./components/Spacer"
  </action>
  <verify>
pnpm f:lib typecheck passes
pnpm f:lib build succeeds
  </verify>
  <done>Spacer component created, both Divider and Spacer exported from package entry point.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib typecheck` passes
- [ ] `pnpm f:lib build` succeeds without errors
- [ ] Divider, Spacer exported from @tosui/react
- [ ] Divider renders horizontal line by default, vertical when specified
- [ ] Divider has proper accessibility attributes
- [ ] Spacer fills flex space correctly (flex: 1)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Phase 1 Layout Primitives complete: Stack, HStack, VStack, Flex, Grid, Container, Divider, Spacer
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-primitives/01-04-SUMMARY.md` with:
- Summary of Phase 1 completion
- All 8 components delivered
- Ready for Phase 2
</output>

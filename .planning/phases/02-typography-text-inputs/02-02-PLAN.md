---
phase: 02-typography-text-inputs
plan: 02
type: execute
---

<objective>
Create Input and Label components for form text entry.

Purpose: Input is the fundamental text input primitive. Label provides accessible form labels with htmlFor support.
Output: Input.tsx, Label.tsx exported from @tosui/react
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@packages/react/src/components/Box/Box.tsx
@packages/react/src/components/Text/Text.tsx
@packages/react/src/index.tsx

**Pattern to follow:**
- Input wraps Box with input-specific styling (border, padding, focus states)
- Uses CSS module for focus/disabled states that can't be done with Box props
- Label wraps Text with htmlFor support
- Both support polymorphic `as` prop (Input defaults to <input>, Label to <label>)
- Spread `...rest` for native input attributes and style overrides
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Input component with CSS module</name>
  <files>packages/react/src/components/Input/Input.tsx, packages/react/src/components/Input/input.module.css, packages/react/src/components/Input/index.tsx</files>
  <action>
Create Input component:
- Props: size ("sm" | "md" | "lg", default "md"), variant ("outline" | "filled", default "outline"), isDisabled, isInvalid
- Default element: <input> via polymorphic `as`
- Size controls height and font size (sm: h=8/text-sm, md: h=10/text-md, lg: h=12/text-lg)
- "outline" variant: border, transparent bg, focus ring
- "filled" variant: surface bg, subtle border, focus ring
- isDisabled: reduced opacity, not-allowed cursor
- isInvalid: error border color
- CSS module for :focus-visible, :disabled, :placeholder states
- Use Box for base styling, add native input props via rest spread
- Export InputOwnProps and InputProps types
  </action>
  <verify>
pnpm f:lib typecheck passes
File exists at packages/react/src/components/Input/Input.tsx
  </verify>
  <done>Input component created with size, variant, isDisabled, isInvalid props.</done>
</task>

<task type="auto">
  <name>Task 2: Create Label component and export both</name>
  <files>packages/react/src/components/Label/Label.tsx, packages/react/src/components/Label/index.tsx, packages/react/src/index.tsx</files>
  <action>
Create Label component:
- Props: size (FontSize), weight (FontWeight), required (boolean to show asterisk)
- Default element: <label> via polymorphic `as`
- Passes through htmlFor for accessibility
- If required, append red asterisk via CSS or inline span
- Simple wrapper around Box with text styling
- Export LabelOwnProps and LabelProps types

Then export both from main index.tsx:
- export { Input, type InputProps, type InputOwnProps } from "./components/Input"
- export { Label, type LabelProps, type LabelOwnProps } from "./components/Label"
  </action>
  <verify>
pnpm f:lib typecheck passes
pnpm f:lib build succeeds
  </verify>
  <done>Label component created, both Input and Label exported from package.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib typecheck` passes
- [ ] `pnpm f:lib build` succeeds without errors
- [ ] Input and Label exported from @tosui/react
- [ ] Input renders as <input> with proper styling
- [ ] Input supports size, variant, isDisabled, isInvalid props
- [ ] Label renders as <label> with htmlFor support
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Input has proper focus states and variants
- Label supports required asterisk indicator
</success_criteria>

<output>
After completion, create `.planning/phases/02-typography-text-inputs/02-02-SUMMARY.md`
</output>

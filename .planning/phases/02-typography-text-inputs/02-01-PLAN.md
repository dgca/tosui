---
phase: 02-typography-text-inputs
plan: 01
type: execute
---

<objective>
Create Code component for inline code styling.

Purpose: Code is a typography primitive for displaying inline code snippets with monospace font and optional visual distinction (background, border).
Output: Code.tsx exported from @tosui/react
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@packages/react/src/components/Text/Text.tsx
@packages/react/src/components/Box/Box.tsx
@packages/react/src/index.tsx

**Pattern to follow:**
- Code wraps Text (or Box) with monospace font family
- Default element: <code>
- Supports size prop for consistent typography scaling
- Optional variant for visual distinction (plain, subtle background)
- Polymorphic `as` prop support
- Spread `...rest` for style overrides
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Code component</name>
  <files>packages/react/src/components/Code/Code.tsx, packages/react/src/components/Code/index.tsx</files>
  <action>
Create Code component that wraps Box with:
- Props: size (FontSize), color (text color), variant ("plain" | "subtle", default "plain")
- Default element: <code> via polymorphic `as`
- fontFamily="mono" always applied
- "subtle" variant adds surface background and sm rounded corners with px={1}
- Export CodeOwnProps and CodeProps types
- Follow Text.tsx pattern for prop structure
- Spread ...rest onto Box for overrides
  </action>
  <verify>
pnpm f:lib typecheck passes
File exists at packages/react/src/components/Code/Code.tsx
  </verify>
  <done>Code component created with size, color, variant props and monospace font.</done>
</task>

<task type="auto">
  <name>Task 2: Export Code from package</name>
  <files>packages/react/src/index.tsx</files>
  <action>
Add Code export to main index.tsx:
- export { Code, type CodeProps, type CodeOwnProps } from "./components/Code"
  </action>
  <verify>
pnpm f:lib typecheck passes
pnpm f:lib build succeeds
  </verify>
  <done>Code component exported from @tosui/react package entry.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib typecheck` passes
- [ ] `pnpm f:lib build` succeeds without errors
- [ ] Code exported from @tosui/react
- [ ] Code renders as <code> by default with monospace font
- [ ] "subtle" variant adds background styling
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Code component displays inline code with monospace font
</success_criteria>

<output>
After completion, create `.planning/phases/02-typography-text-inputs/02-01-SUMMARY.md`
</output>

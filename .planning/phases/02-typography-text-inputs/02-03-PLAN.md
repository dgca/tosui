---
phase: 02-typography-text-inputs
plan: 03
type: execute
---

<objective>
Create Textarea component for multiline text input.

Purpose: Textarea provides multiline text entry with consistent styling matching Input.
Output: Textarea.tsx exported from @tosui/react
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@packages/react/src/components/Input/Input.tsx
@packages/react/src/components/Input/input.module.css
@packages/react/src/index.tsx

**Pattern to follow:**
- Textarea mirrors Input API for consistency
- Uses same CSS module patterns as Input for focus/disabled states
- Default element: <textarea>
- Additional props: rows, resize
- Polymorphic `as` prop support
- Spread `...rest` for native textarea attributes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Textarea component</name>
  <files>packages/react/src/components/Textarea/Textarea.tsx, packages/react/src/components/Textarea/textarea.module.css, packages/react/src/components/Textarea/index.tsx</files>
  <action>
Create Textarea component:
- Props: size ("sm" | "md" | "lg", default "md"), variant ("outline" | "filled", default "outline"), isDisabled, isInvalid, rows (number, default 3), resize ("none" | "vertical" | "horizontal" | "both", default "vertical")
- Default element: <textarea> via polymorphic `as`
- Size controls font size and padding (matches Input sizing feel)
- Variants match Input: outline has border, filled has surface bg
- isDisabled: reduced opacity, not-allowed cursor
- isInvalid: error border color
- CSS module for :focus-visible, :disabled, :placeholder, resize control
- Export TextareaOwnProps and TextareaProps types
  </action>
  <verify>
pnpm f:lib typecheck passes
File exists at packages/react/src/components/Textarea/Textarea.tsx
  </verify>
  <done>Textarea component created matching Input patterns with rows and resize props.</done>
</task>

<task type="auto">
  <name>Task 2: Export Textarea and update App.tsx with Phase 2 examples</name>
  <files>packages/react/src/index.tsx, packages/react/src/App.tsx</files>
  <action>
Export Textarea from main index.tsx:
- export { Textarea, type TextareaProps, type TextareaOwnProps } from "./components/Textarea"

Then add Phase 2 component examples to App.tsx per project constraint:
- Add imports for Code, Input, Label, Textarea
- Add section types and navigation items for each
- Add example sections demonstrating:
  - Code: plain and subtle variants, different sizes
  - Input: sizes, variants, disabled/invalid states
  - Label: with required indicator
  - Textarea: sizes, variants, rows, resize options
  </action>
  <verify>
pnpm f:lib typecheck passes
pnpm f:lib build succeeds
  </verify>
  <done>Textarea exported, App.tsx updated with all Phase 2 component examples.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib typecheck` passes
- [ ] `pnpm f:lib build` succeeds without errors
- [ ] Textarea exported from @tosui/react
- [ ] App.tsx includes examples for Code, Input, Label, Textarea
- [ ] All Phase 2 components visible in dev server
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Textarea has consistent styling with Input
- Phase 2 components demonstrated in App.tsx playground
</success_criteria>

<output>
After completion, create `.planning/phases/02-typography-text-inputs/02-03-SUMMARY.md`
</output>

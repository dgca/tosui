---
phase: 04-buttons-feedback
plan: 04
type: execute
subsystem: ui
---

<objective>
Create Progress component - a progress bar indicator.

Purpose: Show progress of tasks, uploads, or any percentage-based operations.
Output: Progress component with value, color variants, and size options.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files:**
@packages/react/src/components/Box/Box.tsx

**Established patterns:**
- Box wrapper with semantic props
- Color scheme patterns
- Size variants
- CSS Modules for styling

**Constraining decisions:**
- Use existing color tokens
- Native progress element or div with role="progressbar"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Progress component</name>
  <files>packages/react/src/components/Progress/Progress.tsx, packages/react/src/components/Progress/progress.module.css, packages/react/src/components/Progress/index.tsx</files>
  <action>
Create Progress as a progress bar:

1. Progress.tsx:
   - Props: value (0-100), max (default 100), colorScheme (primary/accent/success/warning/error/info, default primary), size (sm/md/lg, default md), isIndeterminate (boolean, for loading without known progress)
   - Size heights: sm=4px, md=8px, lg=12px
   - Track: bg=border-muted, rounded=full
   - Fill: bg={colorScheme}-default, width=value%, rounded=full
   - Indeterminate: animated stripe pattern or sliding animation
   - role="progressbar", aria-valuenow, aria-valuemin, aria-valuemax

2. progress.module.css:
   - Track and fill styles
   - Size variants
   - Indeterminate animation (CSS keyframes)
   - Smooth width transition for determinate

3. index.tsx: Export component and types

Pattern: Standard progress bar with accessible attributes.
  </action>
  <verify>pnpm f:lib build succeeds</verify>
  <done>Progress component created with value-based fill, size variants, indeterminate mode</done>
</task>

<task type="auto">
  <name>Task 2: Export Progress from library</name>
  <files>packages/react/src/index.tsx</files>
  <action>
Add export for Progress:
- export { Progress, type ProgressColorScheme, type ProgressSize, type ProgressProps } from "./components/Progress";
  </action>
  <verify>pnpm f:lib build succeeds, Progress importable from @tosui/react</verify>
  <done>Progress exported from @tosui/react</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib build` succeeds
- [ ] Progress renders with correct width for value
- [ ] All colorSchemes work
- [ ] sm/md/lg sizes display correctly
- [ ] Indeterminate mode animates
- [ ] Accessible with role="progressbar"
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Progress is accessible with ARIA attributes
</success_criteria>

<output>
After completion, create `.planning/phases/04-buttons-feedback/04-04-SUMMARY.md`
</output>

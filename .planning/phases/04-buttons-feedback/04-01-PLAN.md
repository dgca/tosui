---
phase: 04-buttons-feedback
plan: 01
type: execute
subsystem: ui
---

<objective>
Create IconButton component - an icon-only button variant.

Purpose: Provide compact buttons for toolbars and actions where text labels are unnecessary.
Output: IconButton component with size variants, color schemes, and accessibility.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files:**
@packages/react/src/components/Button/Button.tsx
@packages/react/src/components/Button/button.module.css

**Established patterns:**
- Button component with variant, size, colorScheme
- CSS Modules for styling
- Box wrapper with polymorphic support
- sizeConfig objects for size variants

**Constraining decisions:**
- Follow Button API patterns for consistency
- Use existing color scheme types
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IconButton component</name>
  <files>packages/react/src/components/IconButton/IconButton.tsx, packages/react/src/components/IconButton/iconbutton.module.css, packages/react/src/components/IconButton/index.tsx</files>
  <action>
Create IconButton as a Button wrapper optimized for icons:

1. IconButton.tsx:
   - Use forwardRef for ref forwarding
   - Props: icon (ReactNode, required), variant, size (sm/md/lg), colorScheme, disabled, loading
   - aria-label required for accessibility (no visible text)
   - Square sizing: sm=28px, md=36px, lg=44px
   - Reuse Button internally or compose with Box following Button patterns
   - Render icon as child, show Spinner when loading

2. iconbutton.module.css:
   - Minimal styles (Button handles most)
   - Square aspect ratio via CSS

3. index.tsx: Export component and types

Pattern: Follow Button's sizeConfig pattern but with square dimensions.
  </action>
  <verify>pnpm f:lib build succeeds</verify>
  <done>IconButton component created with sm/md/lg sizes, all colorSchemes, loading state</done>
</task>

<task type="auto">
  <name>Task 2: Export IconButton from library</name>
  <files>packages/react/src/index.tsx</files>
  <action>
Add export for IconButton:
- export { IconButton, type IconButtonSize, type IconButtonProps } from "./components/IconButton";
  </action>
  <verify>pnpm f:lib build succeeds, IconButton importable from @tosui/react</verify>
  <done>IconButton exported from @tosui/react</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib build` succeeds
- [ ] IconButton renders with sm/md/lg sizes
- [ ] All colorSchemes work (primary, accent, success, warning, error, info)
- [ ] Loading state shows spinner
- [ ] aria-label is required (TypeScript error without it)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- IconButton is accessible (requires aria-label)
</success_criteria>

<output>
After completion, create `.planning/phases/04-buttons-feedback/04-01-SUMMARY.md`
</output>

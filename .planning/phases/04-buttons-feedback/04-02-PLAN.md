---
phase: 04-buttons-feedback
plan: 02
type: execute
subsystem: ui
---

<objective>
Create Alert component - a status/notification banner.

Purpose: Provide contextual feedback messages for success, warning, error, and info states.
Output: Alert component with status variants, optional icon, title, description, and closable.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files:**
@packages/react/src/components/Box/Box.tsx
@packages/react/src/components/HStack/HStack.tsx

**Established patterns:**
- Box wrapper with semantic props
- Color scheme patterns (success, warning, error, info)
- CSS Modules for styling

**Constraining decisions:**
- Use existing color tokens (success-subtle, error-subtle, etc.)
- Status determines both color and default icon
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Alert component</name>
  <files>packages/react/src/components/Alert/Alert.tsx, packages/react/src/components/Alert/alert.module.css, packages/react/src/components/Alert/index.tsx</files>
  <action>
Create Alert as a status banner:

1. Alert.tsx:
   - Props: status (success/warning/error/info, default info), title (optional), children (description), icon (ReactNode, optional - uses default per status if not provided), onClose (optional callback for X button)
   - Layout: HStack with icon | VStack(title, description) | close button (if onClose)
   - Background: {status}-subtle
   - Border-left: 4px solid {status}-default
   - Icon color: {status}
   - Default icons: success=checkmark, warning=exclamation, error=X circle, info=i circle (simple Unicode or SVG)
   - role="alert" for accessibility

2. alert.module.css:
   - Base alert styles
   - Status color variants via CSS classes
   - Close button styling

3. index.tsx: Export component and types

Pattern: Similar to other UI libraries' Alert patterns but simpler.
  </action>
  <verify>pnpm f:lib build succeeds</verify>
  <done>Alert component created with all status variants, optional title/icon/close</done>
</task>

<task type="auto">
  <name>Task 2: Export Alert from library</name>
  <files>packages/react/src/index.tsx</files>
  <action>
Add export for Alert:
- export { Alert, type AlertStatus, type AlertProps } from "./components/Alert";
  </action>
  <verify>pnpm f:lib build succeeds, Alert importable from @tosui/react</verify>
  <done>Alert exported from @tosui/react</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib build` succeeds
- [ ] Alert renders with all status variants (success, warning, error, info)
- [ ] Optional title displays above description
- [ ] Custom icon overrides default
- [ ] Close button appears when onClose provided
- [ ] role="alert" for screen readers
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Alert is accessible with role="alert"
</success_criteria>

<output>
After completion, create `.planning/phases/04-buttons-feedback/04-02-SUMMARY.md`
</output>

---
phase: 04-buttons-feedback
plan: 03
type: execute
subsystem: ui
---

<objective>
Create Badge component - a small status indicator.

Purpose: Provide compact labels for counts, statuses, or categories.
Output: Badge component with color variants and size options.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files:**
@packages/react/src/components/Box/Box.tsx
@packages/react/src/components/Code/Code.tsx

**Established patterns:**
- Box wrapper with semantic props
- Color scheme patterns
- Size variants (sm/md/lg)
- CSS Modules for styling

**Constraining decisions:**
- Use existing color tokens
- Keep it simple - just colored pills with text
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Badge component</name>
  <files>packages/react/src/components/Badge/Badge.tsx, packages/react/src/components/Badge/badge.module.css, packages/react/src/components/Badge/index.tsx</files>
  <action>
Create Badge as a compact status indicator:

1. Badge.tsx:
   - Props: colorScheme (primary/accent/success/warning/error/info/gray, default gray), variant (solid/subtle, default subtle), size (sm/md, default md), children (text content)
   - Size sm: fontSize xs, px 1.5, py 0.5
   - Size md: fontSize sm, px 2, py 0.5
   - Solid variant: bg={colorScheme}-default, color=foreground-inverted
   - Subtle variant: bg={colorScheme}-subtle, color={colorScheme}
   - rounded="full" for pill shape
   - fontWeight="medium"
   - inline-flex for icon support

2. badge.module.css:
   - Base badge styles
   - Any transitions if needed

3. index.tsx: Export component and types

Pattern: Simple, follows Button colorScheme patterns.
  </action>
  <verify>pnpm f:lib build succeeds</verify>
  <done>Badge component created with color schemes, solid/subtle variants, sm/md sizes</done>
</task>

<task type="auto">
  <name>Task 2: Export Badge from library</name>
  <files>packages/react/src/index.tsx</files>
  <action>
Add export for Badge:
- export { Badge, type BadgeColorScheme, type BadgeVariant, type BadgeSize, type BadgeProps } from "./components/Badge";
  </action>
  <verify>pnpm f:lib build succeeds, Badge importable from @tosui/react</verify>
  <done>Badge exported from @tosui/react</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm f:lib build` succeeds
- [ ] Badge renders with all colorSchemes
- [ ] Solid and subtle variants work correctly
- [ ] sm and md sizes display properly
- [ ] Pill shape (rounded full)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Badge displays correctly in all variants
</success_criteria>

<output>
After completion, create `.planning/phases/04-buttons-feedback/04-03-SUMMARY.md`
</output>
